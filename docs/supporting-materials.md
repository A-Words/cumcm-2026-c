# 支撑材料工作区记录

按用户最新要求，当前压缩包只包含表格和程序，共18个文件：

- 五份题目要求的Excel放在压缩包根目录。
- 13个计算、导出和验算程序放在`scripts/`目录，其中三个主计算文件与论文附录C的807行源码一致。

本文件、README、依赖清单、实验协议、哈希清单及其他资料均不打包。打包程序采用明确的文件白名单，逐文件核对后将验收记录写入工作区的`outputs/verification/supporting-materials-validation.json`。

源程序按工作区原样保留。本阶段只核对所选文件及论文附录的版本一致性；完整运行仍使用工作区已有的数据、依赖和协议，不将当前压缩包描述为独立运行环境。
