# 计算程序源码附录与支撑材料

## 2026-09-13：按三部分组织八个核心程序

代码拆分已提交为 `41a4fb9`。附录C保持三个小节：数据预处理程序（`prepare_data.py`）；公共模型与求解工具（`model.py`、`solve_common.py`）；各问求解与统一运行（`q1.py`、`q2.py`、`q3.py`、`q4.py`、`solve.py`）。每个源码块用文件名作标题，不另设八个小节；仍通过 `\lstinputlisting` 完整读取实际文件。

重新核读本地 `tmp/pdfs/gbt7713.2-taru.pdf` 第8页的GB/T 7713.2-2022第4.4条，沿用其将计算程序详细描述置于附录的依据。三组划分来自本次确认的阅读组织方式，并非标准规定。上游模板、字号、正文结构和数值表格保持原样。

运行 `python scripts/build_paper.py --skip-tables` 后，新PDF共67页，附录C为第46—67页。8个文件共922行，PDF中逐行内容（忽略排版空白）与源码一致，编译快照与源码字节一致；第1—45页逐页渲染与本轮修改前完全一致。22页源码附录已逐页查看缩略图，并放大检查第61页的两文件衔接，无裁剪或重叠；编译无溢出框、缺字或未解析引用。

本次数值与代码拆分验算见 `docs/question-code-layout.md`；本轮附录专项验收见 `outputs/verification/appendix-source-validation.json`。本轮不刷新旧的全量数值验收哈希；`paper-validation.json` 仍绑定上一版PDF，不能作为本版的全量交付验收。

按用户后续确认，移除旧 `supporting-materials.zip`，仅将附录出现的8个代码文件直接放到 `outputs/deliverables/`，不建立代码子目录、不生成压缩包，其他实验和验算脚本不复制到交付目录。8个副本均与 `scripts/` 中的源文件逐字节一致，用户自行选择材料压缩。正式交付前仍须刷新完整验收。

## 2026-09-12 历史记录

用户要求论文附录直接放置计算程序源码，竞赛时以论文和支撑材料压缩包分别提交。本轮将附录C“程序与支撑文件索引”替换为“计算程序源码”，保留附录A的指定日期结果及附录B的补充对照。

## 源码覆盖范围

完整刊载下列三个实际执行文件，共807行，含原注释和空行，不以伪代码、函数签名或省略号替换程序。

| 程序 | 行数 | 作用 |
| --- | ---: | --- |
| `scripts/prepare_data.py` | 231 | 原始附件读取、日期及时间对齐、预报处理、审计与数组生成 |
| `scripts/model.py` | 327 | 四问共用的LP、因果预测、分位修正、储能反馈、连续库存仿真及费用结算 |
| `scripts/solve.py` | 249 | 问题一、1月选参、年度四种主策略、历史基准、预报子集、合约及主流程灵敏度，写出NPZ和工作簿数据 |

三个程序覆盖四问主结果。按用户最新明确要求，当前压缩包只放8个计算及导出程序、5个独立验算程序和根目录的5份结果Excel，共18个文件。说明、依赖清单、协议及哈希清单均移出包，源程序保持原样。当前包用于收集表格和程序；完整运行所需的数据、环境和协议仍保留在工作区。[本项工作区记录](../supporting-materials.md)本身也不纳入压缩包。

## 排版与标准依据

核读本地标准副本`tmp/pdfs/gbt7713.2-taru.pdf`第8页（标准正文第4页）的GB/T 7713.2-2022第4.4条。该条将计算程序的详细描述列为可在附录编排的补充内容，因此将较长源码放在文末，正文继续保留模型、求解与结果论证。原文来源为[塔里木大学托管的标准全文](https://kjc.taru.edu.cn/__local/B/4E/D4/D64707952CDB8ED6D310CED6235_9A986338_6D4C8.pdf)；本轮亦核对了[国家标准公开页](https://openstd.samr.gov.cn/bzgk/std/newGbInfo?hcno=0B963916637B8F34B295FCF4A51A1BE5)的名称和现行状态。公开元数据页不作为条款原文来源。

本轮具体刊载范围来自用户要求，不推断为国标要求所有论文必须附完整代码。沿用上游CUMCMThesis已有的`listings`包，局部`calculations`样式位于`paper/main.tex`。实际PDF代码字为9 pt、行号6.5 pt，正文和表格字号保持原设置；原行号连续、中文字符串可见，长行自动折行，不缩放整个页面。

`paper/sections/appendix.tex`通过`\lstinputlisting`读取原文件。`scripts/build_paper.py`从这些引用发现源码，将原始字节复制到独立编译快照，并在构建记录中保存哈希和行数。源码变化时须重建论文，支撑材料打包器拒绝将旧论文与新源码配套。

## 当前结果与验收

全文64页，附录C第46—64页共19页。第1—45页逐页渲染与修改前一致；正文仍为第2—29页共28页，摘要、AI声明及参考文献计入后，附录前共30页。删除原索引表后，共65张表（正文22、附录43），其中38张题面格式表及全部数值结果保持不变。按用户最新要求，附录C仅保留三个程序的小标题和完整源码；导语、运行环境、支撑包及各程序用途说明已删除，源码保持原样。

执行`python scripts/build_paper.py --skip-tables`构建。此次仅改附录排版和交付组织，沿用哈希未变的计算归档，不重跑年度求解。针对性核验包括：

- 三个源文件与编译快照逐字节一致；PDF中807个原行号完整，逐行提取内容与源码比较通过（比较时忽略排版空白）。
- 656项既有归档聚合检查的来源哈希有效，201项既有补充灵敏度验算的来源哈希有效；独立由归档核对PDF中的576个数值字段通过。
- 全部65张表的三线、字号和位置检查通过；4幅图、40个公式编号、12条参考文献保留，构建无溢出框、缺字或未解析引用。
- 附录全部新增页面经渲染检查，源码中文、缩进、行号、长行折行及分页无裁剪；前45页渲染逐页比较一致。
- 压缩包按文件清单生成并核对所有成员字节、CRC和SHA-256，同时绑定配套论文及附录源码哈希；完整报告见`outputs/verification/`。
- 前一版含结果归档的支撑包已完成解压验算：`validate_results.py`的179项检查通过；`verify_q1_milp.py`得到35126.948589289634元，最优间隙为0。此记录不替代精简包从原始附件重新计算的验证，精简包不再携带这些生成归档。
- 上一版23文件包曾完成干净解压、预处理和问题一重算检查，工作区26项回归测试通过。当前18文件包按用户要求移除说明及运行协议，只核验所选表格与源码的字节、哈希和附录一致性，不沿用上一版独立运行的验收结论；当前验收记录仅保留在工作区。

当前数值核验和版式记录分别见`outputs/verification/paper-validation.json`、`outputs/verification/table-style-validation.json`，打包记录见`outputs/verification/supporting-materials-validation.json`。原始题目、模型程序、主结果、补充实验及五份Excel均未修改。
