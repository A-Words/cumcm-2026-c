\subsection{统一物理模型与线性化}
\subsubsection{能量平衡及储能边界}
每段能量平衡及储电量演化满足
\begin{gather}
 q_t+e_t+V_t+b_t = L_t+c_t+w_t,\label{eq:balance}\\
 S_{t+1} = S_t+\eta_c c_t-b_t/\eta_d,\quad \eta_c=\eta_d=0.9.\label{eq:soc}
\end{gather}
物理边界为
\begin{gather}
 S_{\min}=1200\le S_t\le10800=S_{\max}\quad(t=0,\ldots,144),\notag\\
 0\le c_t,b_t\le P_{\max}\Delta=5000/6,\label{eq:bounds}\\
 q_t,e_t,w_t\ge0,\qquad c_tb_t=0.\label{eq:nonnegative}
\end{gather}
题目要求供电不低于需求，式\eqref{eq:balance}以非负$w_t$将其改写为等式。此时$w_t$也包含已经付款但未利用的常规电量，不能在费用核算中再次扣除。

\subsubsection{充放电互斥与线性规划的适用性}
一般而言，互斥约束会引入二元变量。但在免费不利用余电的假设下，可证明存在同费用的互斥解。对任意同时充放电的可行段，令
\begin{equation}
 \delta=\min\{c_t,b_t/(\eta_c\eta_d)\},\quad
 c'_t=c_t-\delta,\quad b'_t=b_t-\eta_c\eta_d\delta,\quad
 w'_t=w_t+(1-\eta_c\eta_d)\delta.
\end{equation}
该变换保持储电量增量、常规购电量、紧急电量及能量平衡不变，至少一种充放电量变为零，并且不增加功率。因此只要费用不奖励额外循环，就存在与线性松弛等价的互斥最优解。这个结论依赖免费余电处置，不能推广至禁止弃电或给充放电补贴的模型。
