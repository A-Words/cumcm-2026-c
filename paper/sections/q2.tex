\subsection{问题二：固定计划与实时反馈}\label{sec:q2}
\subsubsection{信息条件与决策变量}
本问电价已知，但当天负荷和光伏尚未实现。零时购电量一经确定就需要支付，未使用也不退款；供电缺口按五倍电价补购。因此，制定计划需要权衡提前多购与事后补购的不同代价。

记$\mathcal H_d$为第$d$天零时的历史观测，$\widehat N_{d,t}$为净负荷点预测。规划变量为固定计划$g_{d,t}$及预测充放电$\widehat c_{d,t},\widehat b_{d,t}$、预测库存$\widehat S_{d,t}$、预测余量$\widehat w_{d,t}$。真实动作与状态用$c,b,S,e,w$记录，带帽变量仅用于制定计划。

沿用问题一的效率、母线侧计量、容量和功率约束；改变的是信息条件与日末要求。真实库存从1月1日6000\,kWh起连续承接，预测规划设置日末恢复目标$B=6000$\,kWh，真实执行只受物理边界约束。该目标用于减轻逐日规划的短视，不是题目要求每天实际回到6000\,kWh。

\subsubsection{目标函数与风险净负荷}
评价对象是正式期真实现金支出：
\begin{equation}
 C_2=\sum_{d=31}^{364}\sum_{t=0}^{143}
       \left(p_tg_{d,t}+5p_te_{d,t}\right).\label{eq:q2-bill}
\end{equation}
日期索引从1月1日$d=0$开始。为处理零时未知的$e_{d,t}$，本文由历史残差修正净负荷，在风险输入下最小化当日常规电费，再按真实需求执行反馈。这是对随机购电问题的规划近似。

先说明点预测的构造。有一周以上历史时，利用此前最多四个同星期日的同刻负荷：
\begin{equation}
 \widehat P^{\mathrm L}_{d,t}=
 \frac{\sum_{k=1}^{K_d}0.55^{k-1}P^{\mathrm L}_{d-7k,t}}
      {\sum_{k=1}^{K_d}0.55^{k-1}},\qquad
 K_d=\min\{4,\lfloor d/7\rfloor\}.\label{eq:load-history}
\end{equation}
历史不足7天时改用此前最多7天，以$0.8^{k-1}$衰减加权。光伏采用较短的历史窗口：
\begin{equation}
 \widehat P^{\mathrm{V,H}}_{d,t}=
 \frac{\sum_{k=1}^{\min(7,d)}0.75^{k-1}P^{\mathrm V}_{d-k,t}}
      {\sum_{k=1}^{\min(7,d)}0.75^{k-1}},\qquad d\ge1.
\end{equation}
首日没有历史，负荷取4500\,kW、光伏取0，只用于初始化。两类功率预测相减后乘$\Delta$，得到$\widehat N_{d,t}$。附件一典型负荷几乎等于全年同刻均值，故不将其作为本问先验，否则零时预测会间接含有未来样本。

对每个已完成日期$j$，保存当时预测与实际净负荷的残差$R_{j,t}=N_{j,t}-\widehat N_{j,t}$。在$\max(7,d-28)\le j<d$内，将同刻及前后各3段残差合并，记为$\mathcal R_{d,t}$；区间端点以最近端点复制补齐。规划输入为
\begin{equation}
 \widetilde N_{d,t}=\widehat N_{d,t}+Q_{\alpha}(\mathcal R_{d,t}).\label{eq:risk}
\end{equation}
分位数采用排序样本间线性插值，无可用历史残差时不添加修正。较大的$\alpha$通常使提前备电更多，但可能增加已经付费的未利用电量。该经验分位只修正各段规划输入，既没有构造联合机会约束，也不表示系统具有$\alpha$的供电可靠率。

由此，零时实际求解的目标为
\begin{equation}
 \min \widehat C_{2,d}=\sum_{t=0}^{143}p_tg_{d,t}.\label{eq:q2-objective}
\end{equation}
它与式\eqref{eq:q2-bill}作用不同：前者是在风险输入下选择计划的优化目标，后者是在实际路径上比较策略的评价指标。分位参数应按后者的历史费用选择，不能仅按预测误差或规划目标选择。

\subsubsection{约束条件与汇总模型}
\textbf{a) 预测供需平衡。}以$\widetilde N_{d,t}$替代未知的实际净负荷，要求
\begin{equation}
 g_{d,t}+\widehat b_{d,t}=\widetilde N_{d,t}+\widehat c_{d,t}+\widehat w_{d,t}.
\end{equation}
本阶段不设置免费的缺口变量；当预测供电不足时必须通过$g$补足。实际紧急电留到交付阶段按五倍电价计费。

\textbf{b) 储能递推与边界。}预测态使用式\eqref{eq:soc}的同一效率关系，并满足$0\le\widehat c,\widehat b\le5000/6$、$1200\le\widehat S\le10800$。允许未利用余量，故可采用问题一证明的线性松弛。

\textbf{c) 起止库存。}以真实到达的日初库存作为规划起点，而不是每日重新初始化：
\begin{equation}
 \widehat S_{d,0}=S_{d,0},\qquad \widehat S_{d,144}\ge B,\qquad B=6000.
\end{equation}
预测下保留库存，可以减轻逐日规划用尽储能的问题。已有两阶段调度研究也将日前库存边界传入日内决策\upcite{hu2025emergency}，但本文的6000\,kWh是自行设置的恢复目标，并非由该研究给出的最优参数。

\textbf{d) 固定计划和可用信息。}全天$q_{d,t}=g_{d,t}$，交付前不能通过常规渠道追加或撤销；$g_d$只能是$\mathcal H_d$及真实日初库存的函数。用当天真值修正零时计划，会改变本问题的信息条件。

将上述关系合并，省略日期下标后得到
\begin{equation}
\begin{aligned}[b]
 (\mathrm P_2)\quad \min\quad&\sum_{t=0}^{143}p_tg_t,\\
 \mathrm{s.t.}\quad&g_t+\widehat b_t=\widetilde N_t+\widehat c_t+\widehat w_t,\\
 &\widehat S_{t+1}=\widehat S_t+0.9\widehat c_t-\widehat b_t/0.9,\\
 &0\le\widehat c_t,\widehat b_t\le5000/6,\quad g_t,\widehat w_t\ge0,\\
 &1200\le\widehat S_t\le10800,\\
 &\widehat S_0=S_{d,0},\quad\widehat S_{144}\ge6000.
\end{aligned}\label{eq:q2-summary}
\end{equation}
所有区间约束作用于$t=0,\ldots,143$，库存边界包括末端。得到计划后，预测库存不再覆盖真实状态，次日规划继续从实际运行结果开始。

\subsubsection{参数选择与执行算法}
候选分位为$\{0.50,0.65,0.70,0.80,0.90,0.95\}$。每个候选从1月1日6000\,kWh起独立回放，仅以1月15—31日真实费用评分，选中$\alpha=0.80$。正式策略的1月运行预先固定使用0.80，2月起仍使用该参数；历史窗口随日期滚动，但不按正式期费用重新选参。

当段实际负荷和光伏到达后，定义常规电力相对净需求的余量$z_t=g_t-(L_t-V_t)$。若$z_t\ge0$，尽可能将余量存入电池：
\begin{equation}
\begin{aligned}[b]
 c_t&=\min\{z_t,5000/6,(10800-S_t)/0.9\},\\
 b_t&=e_t=0,\qquad w_t=z_t-c_t.
\end{aligned}\label{eq:feedback-surplus}
\end{equation}
若$z_t<0$，先在功率与库存允许范围内放电，其余缺口紧急购入：
\begin{equation}
\begin{aligned}[b]
 b_t&=\min\{-z_t,5000/6,0.9(S_t-1200)\},\\
 c_t&=w_t=0,\qquad e_t=-z_t-b_t.
\end{aligned}\label{eq:feedback-shortage}
\end{equation}
两个最小值同时限制当段余缺、功率和可用库存，再按式\eqref{eq:soc}更新状态，保证平衡与安全边界。题目未限制紧急购电容量，因此剩余缺口均能补足。

完整的日循环算法如下：
\begin{enumerate}
 \item 在零时读取前一日末库存和已完成日期观测，计算当日点预测。
 \item 按固定$\alpha$提取历史残差分位，构造144段风险净负荷。
 \item 求解$(\mathrm P_2)$，锁定全天$g_t$；保留预测态仅供诊断。
 \item 在每个交付区间接收实际净负荷，按式\eqref{eq:feedback-surplus}或\eqref{eq:feedback-shortage}执行，并更新真实库存。
 \item 对原计划和实际紧急电分别计费，检查当段供需平衡；日末库存直接传入次日。
 \item 累计2—12月费用，并汇总指定日期的购电计划、储能运行与紧急购电事件。
\end{enumerate}
该反馈优先补足当前缺口，未比较未来紧急电价；后文以价格感知反馈检验这一选择。

\subsubsection{全年与指定日期结果分析}
正式期334天总费用为\PaperValue{q2-cost}\,元，其中紧急电费\PaperValue{q2-emergency-cost}\,元，约占总费的4.43\%。表\ref{tab:q2-summary}给出费用和电量分解，表\ref{tab:q2-days-summary}汇总四个指定日期。正文按题面表1、表2展示9月23日，其他日期及四日期紧急事件、账单见附录\ref{app:q2-days}，与\texttt{result2.xlsx}对应。
\input{generated/q2-summary}
\input{generated/q2-days-summary}
\FloatBarrier
各日期购电表中的购电量为零时确定的常规计划，“全天购电费”为该常规计划费用；紧急购电另列，含紧急费的总支出见全天账单。电量单位均为kWh，费用单位为元。储能表按六个四小时区间求和，并列当日0:00与24:00的真实库存。表内保留两位小数，计算使用完整精度，逐项舍入后可能产生0.01的加总差。

附录紧急购电表将大于$10^{-7}$\,kWh的连续十分钟段合并为事件，终点为最后一段结束时刻；“无、0.00”表示当天没有紧急购电，后续空白表示没有更多事件。24:00与0:00+1含义相同。后续各问沿用上述口径。
\input{generated/representative-day-q2}
四个指定日期中，9月23日出现892.40\,kWh紧急购电，其余三个日期没有。该日总费47814.59元，其中5734.95元来自紧急购电。图\ref{fig:q2-day}选取这一日期展示风险计划、实际净需求及库存的关系，便于检查缺口发生的位置。
\begin{figure}[!htbp]
 \centering\includegraphics[width=\textwidth]{../outputs/figures/paper-q2-day.pdf}
 \caption{问题二在9月23日的固定计划与实际执行}\label{fig:q2-day}
\end{figure}
\FloatBarrier
常规富余电先存入电池，缺口先由电池补给，超出允许放电量后才使用紧急电。因此，同样的净负荷低估，在库存充足的低价段和接近放电边界的高价段会形成不同费用。

全年未利用电量\PaperValue{q2-spill-kwh}\,kWh包含未消纳的付费常规电，不能全部称作弃光。全年115天发生紧急购电；期初、期末库存为\PaperValue{q2-start-soc}\,kWh、\PaperValue{q2-end-soc}\,kWh，按实际现金计费，不对库存另作估值。

\subsubsection{风险参数灵敏度检验}
固定预测方法、反馈规则和共同1月热启动，将正式期分位改为0.50、0.70和0.90，与主参数0.80比较。表\ref{tab:q2-risk-sensitivity}同时列出总费、紧急电量及年末库存，避免只以紧急电量下降判断参数改进。
\input{generated/q2-risk-sensitivity}
\FloatBarrier
分位从0.50提高至0.80，提前备电减少了高价补购，总费下降；继续提高至0.90，紧急电量虽进一步下降，总费却增加约23.22万元，说明常规备电成本已超过新增的补购节省。主参数在所列候选中较省，但不意味着连续参数最优；各方案的年末库存不同，结论限于表中的现金路径。后续增加调整机会时，应重新按历史费用校准。
\label{end:q2}
